{
  "name": "andy-booking-app",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,admin -c blue,magenta \"npm run dev --workspace=apps/backend\" \"npm start --workspace=apps/admin\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
